# 基于 Codex 的 PPT 双渲染 MVP 需求文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | PPT 双渲染 MVP 需求文档 |
| 版本 | 0.1.0 |
| 状态 | MVP 需求基线 |
| 目标 | 基于同一份结构化演示文稿规格，同时生成可浏览的 HTML 演示和可编辑的原生 PPTX |
| 运行方式 | 本地 Codex 开发、本地命令行执行 |
| 关联方案 | `PPT双渲染MVP方案.md` |
| 关联结构技能 | 当前目录 `SKILL.md`，作为可选的结构审查模块 |

## 2. 背景与问题

当前使用 Codex 制作 PPT，容易出现以下问题：

- 内容、叙事、版式和导出逻辑混在一起，修改一处会牵动多个输出格式。
- HTML 可以快速表达复杂视觉，但不能天然保证 PowerPoint 中的可编辑性。
- 直接生成 PPTX 容易出现文字溢出、对象重叠、连接线遮挡、字体替换和页面密度过高。
- 仅凭图片或整页截图生成 PPTX，视觉上可能接近，但无法满足后续编辑要求。
- 缺少统一的结构描述，导致 HTML、PPTX 和 QA 结果之间难以对齐。

本 MVP 采用“一份结构化规格、两个渲染器、一套质量检查”的路线：

```text
输入主题 / Markdown
        ↓
Codex：内容分析、提纲、叙事结构
        ↓
deck.spec.json
    ┌───┴────┐
    ↓        ↓
HTML 渲染器  PPTX 渲染器
    └───┬────┘
        ↓
截图、溢出检查、结构检查、人工复核
```

## 3. 产品目标

### 3.1 MVP 目标

在本地完成一条可重复执行的生成链路：

1. 接收主题或 Markdown 输入。
2. 生成 3–5 页有明确叙事结构的演示文稿规格 `deck.spec.json`。
3. 从同一份规格生成 HTML 演示。
4. 从同一份规格生成原生可编辑 PPTX。
5. 对 HTML 和 PPTX 进行截图、结构、溢出、重叠和空白页检查。
6. 输出可追踪的 QA 报告，并支持 Codex 根据报告进行一次或多次修订。

### 3.2 成功标准

MVP 完成后，开发者能够使用一条命令生成一个演示样例，并获得：

- 一份可校验的 `deck.spec.json`。
- 一个可用键盘、滚轮和触控切换的 HTML 演示。
- 一个可以在 PowerPoint 或兼容软件中打开的 PPTX。
- PPTX 中的标题、正文、形状、表格和图表主体为原生对象，而不是整页截图。
- HTML 与 PPTX 在页面顺序、标题和核心内容上保持一致。
- 一份包含问题等级、页面、元素和建议的 QA 报告。

## 4. 用户与使用场景

### 4.1 用户角色

| 角色 | 需求 |
|---|---|
| 内容作者 | 用自然语言或 Markdown 描述主题，获得一份有逻辑的演示稿 |
| PPT 制作者 | 在 PowerPoint 中继续编辑文字、形状、表格和图表 |
| 开发者 | 添加布局、主题、元素类型和质量规则 |
| 审阅者 | 快速查看 HTML、截图蒙版和 QA 报告，定位需要修改的页面 |

### 4.2 典型场景

#### 场景 A：主题生成演示稿

用户输入一个主题和少量背景信息，Codex 生成 3–5 页演示稿，包括标题页、核心流程和对比页。

#### 场景 B：从 Markdown 生成双格式输出

用户在 `examples/demo.md` 中维护内容，系统将其转为统一规格，再生成 `presentation.html` 和 `presentation.pptx`。

#### 场景 C：根据 QA 报告修订

系统发现某页标题溢出或两个卡片重叠，Codex 根据 `qa/report.json` 修改规格或布局参数，再重新渲染。

#### 场景 D：开发新布局

开发者新增一个布局模块，只需定义布局所需的槽位、约束和两个渲染器的实现，不改变上游内容规格。

## 5. 范围定义

### 5.1 MVP 必须包含

- Markdown / 主题文本输入。
- 叙事提纲和页面目标生成。
- 严格 JSON 规格生成与 JSON Schema 校验。
- `title`、`pipeline`、`comparison` 三种首批布局。
- 文本、形状、图片占位、连接线、简单表格和简单图表。
- HTML 演示生成和基本导航。
- 原生可编辑 PPTX 生成。
- HTML 和 PPTX 的页面截图。
- 基础溢出、重叠、空白页和结构一致性检查。
- JSON 和 Markdown 两种 QA 报告。
- 本地命令行入口。

### 5.2 MVP 不包含

- 任意 HTML/CSS 到 PPTX 的无损转换。
- 复杂 CSS：`backdrop-filter`、`mix-blend-mode`、3D 变换、复杂滤镜和复杂动画。
- PPTX 的完整导入、反向解析和往返保持。
- 多人协作、云端存储、账号体系和在线编辑器。
- HTML 与 PPTX 之间完全一致的动画效果。
- 复杂视频、音频、交互式数据应用和外部实时数据源。
- 自动达到商业级品牌规范的最终美术稿。

## 6. 功能需求

### 6.1 输入与任务配置

| 编号 | 需求 | 优先级 | 验收方式 |
|---|---|---:|---|
| FR-001 | 系统应接受 Markdown 文件作为主要输入 | P0 | 对 `examples/demo.md` 执行生成命令成功 |
| FR-002 | 系统应支持通过 CLI 参数指定输出目录和输出格式 | P0 | 分别指定 `html`、`pptx`、`both` 并检查产物 |
| FR-003 | 输入缺失、文件不存在或格式不支持时，应返回可读错误 | P0 | 构造错误输入并检查退出码和错误信息 |
| FR-004 | 任务应记录输入文件、运行时间、规格版本和渲染模式 | P1 | 在产物元数据或 QA 报告中可追踪 |

### 6.2 内容分析与叙事规划

| 编号 | 需求 | 优先级 | 验收方式 |
|---|---|---:|---|
| FR-005 | Codex 应将输入内容拆解为演示目标、受众、语言和页面数量 | P0 | 规格中存在对应字段 |
| FR-006 | 每页应有唯一的 `slide_goal` 和 `primary_claim` | P0 | Schema 校验通过且字段非空 |
| FR-007 | 页面应形成明确叙事弧，例如问题—机制—方案—行动 | P0 | 规格存在 `narrative.arc`，人工抽检阅读顺序 |
| FR-008 | 规划阶段应优先控制页面密度，而不是把所有原文直接放入页面 | P0 | 超过密度阈值时产生 review flag 或规划失败 |
| FR-009 | 规划结果应能被人工编辑后继续进入渲染流程 | P0 | 手工修改 `deck.spec.json` 后可重新渲染 |

### 6.3 结构化规格

| 编号 | 需求 | 优先级 | 验收方式 |
|---|---|---:|---|
| FR-010 | 系统应生成独立的 `deck.spec.json` | P0 | 输出文件存在且为合法 JSON |
| FR-011 | 系统应使用 `schemas/deck.schema.json` 校验规格 | P0 | 非法规格无法进入渲染阶段 |
| FR-012 | 规格应表达主题、叙事、主题样式、布局、元素和审查标记 | P0 | 检查字段集合和 Schema |
| FR-013 | 元素应包含稳定 ID、类型、角色、内容或引用、可编辑性信息 | P0 | 每个元素均可定位和追踪 |
| FR-014 | 系统应支持 `native`、`asset`、`html_only`、`pptx_only` 四种渲染模式 | P1 | 各模式按约定进入对应渲染器 |
| FR-015 | 规格版本变更应可识别，未知版本不得静默渲染 | P1 | 使用未知版本进行校验并得到明确错误 |

### 6.4 首批布局

| 编号 | 需求 | 优先级 | 验收方式 |
|---|---|---:|---|
| FR-016 | `title` 布局应支持标题、副标题、标签和主视觉占位 | P0 | 生成标题页并检查双输出 |
| FR-017 | `pipeline` 布局应支持步骤节点、连接线、步骤说明和结果区域 | P0 | 生成流程页，检查连接线在节点下方且语义清晰 |
| FR-018 | `comparison` 布局应支持两列或三列对比、结论高亮和差异标记 | P0 | 生成对比页，检查列对齐和结论层级 |
| FR-019 | 布局应在元素超出可用区域时返回结构化问题，而不是静默裁剪 | P0 | 制造超长标题或过多元素并检查报告 |
| FR-020 | 布局模块不应把具体内容逻辑复制到两个渲染器中 | P1 | 检查两端均消费同一份规格和布局槽位 |

### 6.5 HTML 演示输出

| 编号 | 需求 | 优先级 | 验收方式 |
|---|---|---:|---|
| FR-021 | 系统应生成固定 16:9 的 HTML 演示 | P0 | 浏览器打开后页面比例稳定 |
| FR-022 | 每页应作为独立的 `<section class="slide">` 或等价语义容器 | P0 | DOM 检查页面数量与规格一致 |
| FR-023 | 应支持上一页、下一页、页码/导航点和键盘操作 | P0 | 测试方向键、空格、滚轮和导航点 |
| FR-024 | 应支持有限的 CSS reveal 动画，并尊重 `prefers-reduced-motion` | P1 | 检查动画和减少动效设置 |
| FR-025 | 文本、列表和关键图表应优先使用语义 HTML、SVG 或 CSS 表达 | P1 | 检查关键内容不是单一位图 |
| FR-026 | HTML 应能在目标浏览器中稳定截图 | P0 | 批量截图成功且页面尺寸一致 |

### 6.6 PPTX 输出

| 编号 | 需求 | 优先级 | 验收方式 |
|---|---|---:|---|
| FR-027 | 系统应从同一份 `deck.spec.json` 生成 PPTX | P0 | 双渲染输出的页面数和核心内容一致 |
| FR-028 | 标题、正文、形状、简单表格和图表应尽可能生成原生对象 | P0 | 解压/对象检查或 PowerPoint 手工编辑检查 |
| FR-029 | PPTX 不得以整页截图作为默认交付方式 | P0 | 检查页面对象数量和可编辑性 |
| FR-030 | PPTX 应使用统一主题令牌，包括字体、颜色、间距和图表色板 | P0 | 修改 `theme.json` 后重新生成并观察双输出 |
| FR-031 | 连接线应在节点之前绘制，避免覆盖节点文字和边框 | P0 | 流程页视觉检查和对象顺序检查 |
| FR-032 | PPTX 应在目标 Office/兼容软件中可打开 | P0 | 打开、保存、再次打开不产生修复提示 |
| FR-033 | 对不适合原生化的元素，规格应明确标记为 `asset` 或 `pptx_only` | P1 | 检查渲染模式和 QA 报告 |

### 6.7 质量检查与修订

| 编号 | 需求 | 优先级 | 验收方式 |
|---|---|---:|---|
| FR-034 | 系统应检查页面数量、标题顺序和核心内容一致性 | P0 | 故意删除页面或标题后检查失败 |
| FR-035 | 系统应检查文字溢出、元素越界和明显重叠 | P0 | 制造边界和重叠问题并生成 issue |
| FR-036 | 系统应检查空白页、空元素和缺少关键标题 | P0 | 构造空页并检查报告 |
| FR-037 | 每个问题应包含严重级别、页面 ID、元素 ID、规则 ID 和修复建议 | P0 | 检查 `qa/report.json` 字段 |
| FR-038 | 系统应输出可读的 Markdown QA 报告 | P0 | 检查 `qa/report.md` 内容完整 |
| FR-039 | Codex 应能够根据 QA 报告定位修改目标并重新生成 | P1 | 完成一次“发现—修改—复测”闭环 |
| FR-040 | 人工复核结果应可记录，不得被自动 QA 结果覆盖 | P1 | 在报告中保留人工 review 状态和备注 |

### 6.8 CLI

| 编号 | 命令 | 说明 |
|---|---|---|
| FR-041 | `node src/cli.mjs plan --input <file>` | 只生成或预览叙事规划 |
| FR-042 | `node src/cli.mjs generate --input <file> --out <dir> --format both` | 执行完整生成链路 |
| FR-043 | `node src/cli.mjs validate --spec <file>` | 校验规格，不渲染 |
| FR-044 | `node src/cli.mjs render --spec <file> --format both` | 根据既有规格渲染输出 |
| FR-045 | `node src/cli.mjs qa --input <dir>` | 对既有输出执行质量检查 |

## 7. 规格与输出契约

### 7.1 规格最低字段

```json
{
  "spec_version": "0.1.0",
  "deck": {
    "title": "示例标题",
    "purpose": "说明要解决的问题",
    "audience": "目标受众",
    "language": "zh-CN",
    "aspect_ratio": "16:9"
  },
  "theme": "default",
  "narrative": {
    "arc": "问题—机制—方案—行动",
    "key_message": "一句话核心结论"
  },
  "slides": [
    {
      "id": "slide-001",
      "layout": "title",
      "slide_goal": "建立问题背景",
      "primary_claim": "一句话页面结论",
      "elements": [
        {
          "id": "title-001",
          "kind": "text",
          "role": "title",
          "text": "页面标题",
          "editable": true,
          "render_mode": "native"
        }
      ],
      "review_flags": []
    }
  ]
}
```

### 7.2 输出目录契约

```text
dist/<deck-name>/
├── deck.spec.json
├── presentation.html
├── presentation.pptx
├── preview/
│   ├── html-001.png
│   ├── pptx-001.png
│   └── montage.png
└── qa/
    ├── report.json
    └── report.md
```

如果只请求一种格式，未请求的格式可以不生成；但 `deck.spec.json` 和 QA 目录仍应保留。

## 8. 非功能需求

| 编号 | 需求 | 验收方式 |
|---|---|---|
| NFR-001 | 本地优先，不依赖在线编辑器或云端存储 | 断网时仍可运行不需要外部服务的流程 |
| NFR-002 | 同一输入、规格和主题应尽量生成一致结果 | 重复运行并比较结构化输出 |
| NFR-003 | 渲染器不得偷偷改变页面语义和元素内容 | 对比渲染前后的 ID、标题和核心文本 |
| NFR-004 | 规格、主题和布局应可独立演进 | 修改主题不需要修改规划器 |
| NFR-005 | 失败应尽早发生，并给出页面/元素级错误 | 非法规格、缺字体、截图失败均有明确错误 |
| NFR-006 | 关键文字在 HTML 和 PPTX 中应保持可读，避免过小字号和过高密度 | QA 规则加人工抽检 |
| NFR-007 | HTML 应提供基本语义结构、焦点可达性和减少动效支持 | 浏览器键盘操作和辅助设置检查 |
| NFR-008 | 产物应可追踪到输入、规格版本、布局和主题 | 报告和元数据检查 |
| NFR-009 | 运行命令应支持 Windows 本地开发环境 | 在当前工作区执行完整命令 |

## 9. 质量与验收标准

### 9.1 P0 发布门槛

- [ ] `examples/demo.md` 可以成功生成 `deck.spec.json`、HTML 和 PPTX。
- [ ] 规格通过 JSON Schema 校验；非法规格会阻断渲染。
- [ ] HTML 页数、顺序、标题和核心内容与规格一致。
- [ ] HTML 可通过键盘切换页面，并可批量截图。
- [ ] PPTX 可打开，不是整页截图，标题和主要正文可编辑。
- [ ] 三种首批布局均能生成双格式输出。
- [ ] 无明显文字裁切、元素越界、关键内容重叠或空白页。
- [ ] QA 报告能定位至少一种溢出问题、一种结构问题和一种视觉问题。
- [ ] 失败任务返回非零退出码，并说明修复方向。

### 9.2 建议目标

以下是 MVP 的内部目标，不是第三方标准：

- 页面数量、标题顺序和核心文本一致率达到 100%。
- 标题、正文、形状、表格和简单图表等核心对象中，至少 80% 为 PPTX 原生对象。
- 10 页以内的演示稿在本地可稳定完成双渲染和截图。
- 每个页面至少有一个明确主张，正文密度不超过布局阈值。

### 9.3 验收测试矩阵

| 测试类型 | 输入 | 预期结果 |
|---|---|---|
| 正常生成 | 3 页 Markdown | 双格式产物完整生成 |
| 长标题 | 超过标题槽位宽度 | 产生溢出 issue 或触发降级策略 |
| 长正文 | 超过正文密度阈值 | 产生密度 issue，不静默裁切 |
| 非法规格 | 缺少 `slides` 或未知 `layout` | Schema 校验失败，禁止渲染 |
| 空页面 | `elements` 为空 | QA 报告标记高严重度问题 |
| 重叠页面 | 两元素矩形相交 | QA 报告包含页面和元素 ID |
| PPTX 可编辑性 | 修改标题文字和形状 | PowerPoint 中可单独选择和修改 |
| 双输出一致性 | 同一 `deck.spec.json` | 页数、顺序、标题和核心内容一致 |
| 降级元素 | `render_mode=asset` | 输出可见，同时报告可编辑性边界 |

## 10. 当前目录 `SKILL.md` 的产品定位

当前 `SKILL.md` 不应被当作 PPTX 生成器，也不应直接替代布局和渲染模块。它适合复用为“结构审查规则集”，主要提供：

- 一页只保留一个主要信息。
- 明确标题、入口、阅读顺序和视觉层级。
- 使用分组和空间关系表达结构，不只依赖颜色。
- 统一连接线语义，避免箭头含义混乱。
- 控制文本角色和页面密度。
- 在生成后保留 `review_flags`，执行审查—修改—复测循环。

需要适配或暂不直接采用的部分：

- 期刊投稿尺寸、科学图形尺寸和论文版式约束。
- 只针对 scientific figure 的图形分类。
- graphical abstract、期刊合规和科学图注要求。

建议将其映射到 `deck.spec.json` 的 `slide_goal`、`primary_claim`、`reading_order`、`groups`、`edges`、`hierarchy`、`style_constraints`、`accessibility_constraints` 和 `review_flags` 字段。

## 11. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| HTML 与 PPTX 的 CSS 能力不对等 | 两种输出视觉差异大 | 以双渲染为主，限制 HTML 到 PPTX 的安全子集 |
| 字体在本地或 Office 环境缺失 | 换行和版式漂移 | 固定字体栈，启动前检测字体，QA 标记替代字体 |
| AI 生成文字过长 | 溢出、密度过高 | 规划阶段限长，渲染前做测量和 review flag |
| 复杂图表难以编辑 | PPTX 可编辑性下降 | MVP 只支持简单图表，复杂图表明确使用 asset |
| 截图工具不可用 | 无法自动做视觉回归 | 预检浏览器和 LibreOffice，缺失时输出可读阻断信息 |
| 过度依赖当前 SKILL.md | 商业演示被科学图形规则限制 | 只复用结构审查层，保留通用 deck schema |

## 12. 后续版本候选

- 增加 `overview`、`timeline`、`data-story`、`quote` 等布局。
- 增加图片检索、裁切、替代文本和版权来源字段。
- 为 PPTX 增加主题母版和品牌组件库。
- 引入 HTML→PPTX 适配器的 10 页基准集和自动评分。
- 支持 PDF、DOCX、PPTX 的文本抽取作为输入。
- 增加人工审阅界面或本地预览器。
- 引入版本迁移工具和更完整的 `deck.schema.json` 兼容策略。
- 探索 Presenton、PPTAgent、PPT Master、PPTist 等方案中的内容规划、模板和编辑器能力。

## 13. 参考项目与资料

- [PptxGenJS](https://github.com/gitbrent/PptxGenJS)：可编辑 PPTX 的 JavaScript 生成方案。
- [dom-to-pptx](https://github.com/atharva9167j/dom-to-pptx)：HTML DOM 到 PPTX 的实验性转换方案。
- [html2pptx](https://github.com/abdelkrimkr/html2pptx)：HTML 到 PPTX 的实验性转换方案。
- [Presenton](https://github.com/presenton/presenton)：开源 AI 演示文稿应用参考。
- [PPTAgent](https://github.com/icip-cas/PPTAgent)：AI 驱动 PPT 生成研究/工程参考。
- [PPT Master](https://github.com/hugohe3/ppt-master)：自动化 PPT 生成参考。
- [PPTist](https://github.com/pipipi-pikachu/PPTist)：Web PPT 编辑器和可编辑画布参考。
- [Open Presentation Format](https://www.openpresentation.org/)：结构化演示文稿格式方向参考。
- [Slidev 导出](https://sli.dev/guide/exporting.html)、[Marp CLI](https://github.com/marp-team/marp-cli)、[reveal.js](https://revealjs.com/)：HTML/Markdown 演示参考，但不作为 MVP 的原生 PPTX 主路径。
