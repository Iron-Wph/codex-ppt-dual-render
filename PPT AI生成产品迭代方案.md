# PPT AI 生成产品迭代方案

## 1. 文档定位

本文档针对当前 MVP 的核心问题：生成结果虽然能够同时输出 HTML 和可编辑 PPTX，但不同任务几乎复用同一套风格、排版和背景，且论文 PPT 缺少真实图表、插图与视觉证据。

产品下一阶段不应继续堆叠更多文字模板，而应升级为：

> 一个“内容理解、视觉决策、资产引用、可控编辑、双格式输出”一体化的 PPT 创作工作台。

产品目标不是让每次生成都完全随机，而是让用户能够选择风格、看到明显差异、锁定满意部分、局部重做，并且知道每个结论来自哪里。

## 2. 当前问题诊断

### 2.1 用户感知问题

用户目前会形成三种负面判断：

1. “这只是把论文套进固定模板。”
2. “AI 没有理解我的材料，只是在排版文字。”
3. “每次重新生成都差不多，我没有继续尝试的动力。”

### 2.2 当前 MVP 的技术原因

- `schema` 主要支持 `title`、`pipeline`、`comparison` 三种布局。
- PPTX 渲染器固定了背景色、字体、装饰图形、卡片位置和字号。
- Codex 目前主要规划文字内容，没有生成 `style brief` 和页面视觉策略。
- PDF 流程主要提取文字，没有提取 Figure、表格、图片和图注。
- schema 尚未完整支持 `image`、`chart`、`table`、`caption` 等视觉元素。
- 用户无法锁定某一页或某个元素后进行局部重生成。
- 质量检查主要判断几何溢出，尚未评价“视觉丰富度、风格差异和内容证据覆盖率”。

## 3. 产品定位与目标用户

### 3.1 产品定位

面向研究汇报、技术方案、产品汇报和管理层汇报的本地 AI PPT 工作台：

> 输入文档，生成有证据、有风格、可编辑、可迭代的演示文稿。

### 3.2 核心用户

| 用户 | 主要任务 | 关键诉求 |
| --- | --- | --- |
| 研究生/科研人员 | 论文答辩、组会、会议汇报 | 论文图表可引用、结论有来源 |
| 算法/工程师 | 技术方案、实验复盘 | 复杂流程清晰、图表可编辑 |
| 产品经理 | 方案评审、路线汇报 | 叙事结构强、风格专业、可快速修改 |
| 管理者/咨询人员 | 决策汇报 | 信息密度可控、重点突出、视觉可信 |

## 4. 产品原则

### 原则一：风格可控，而不是随机换皮

用户先选择风格方向，AI 在风格边界内生成。系统提供风格预览和“保持风格，只重做内容”的能力。

### 原则二：每一页必须有视觉角色

页面不能只有文字卡片。视觉角色至少包括：论文原图、数据图表、流程图、时间线、矩阵、关键数字、引用卡片或生成插图。

### 原则三：内容证据优先于装饰

论文图表优先使用原文资产；没有合适原图时再生成图形；装饰性背景不能替代证据。

### 原则四：局部编辑优先于整套重生成

用户可以只修改标题、只更换图片、只调整风格、只重排某页，而不破坏已经满意的部分。

### 原则五：所有 AI 输出可验证

数字、结论、图片和图注需要关联来源页；结构与视觉检查都必须可回放、可解释。

## 5. 目标产品体验

### 5.1 首次生成流程

```text
上传 PDF / Markdown
  ↓
选择用途：答辩 / 组会 / 技术方案 / 管理汇报
  ↓
选择风格：学术极简 / 科技深色 / 白底编辑 / 杂志叙事 / 数据仪表盘 / 品牌风
  ↓
系统展示风格预览与页面规划
  ↓
Codex 生成：内容大纲 + style brief + 视觉资产计划
  ↓
抽取论文图表并匹配到页面
  ↓
HTML 快速预览 + PPTX 可编辑导出
  ↓
用户锁定、局部修改、再次导出
```

### 5.2 论文模式的目标页面结构

论文模式默认仍保持 8–10 页，但页面视觉不再固定：

1. 封面：标题 + 论文核心视觉或关键图。
2. 研究问题：问题场景图、对比示意或关键数字。
3. 方法总览：流程图或系统结构图。
4. 核心算法：伪代码、机制图或并行模块图。
5. 实验设置：数据集、任务、模型和评测流程。
6. 主结果：论文原图或重绘后的可编辑图表。
7. 消融/效率：对齐的柱状图、折线图或表格。
8. 泛化与边界：真实场景图、失败案例或能力边界图。
9. 结论：三条结论 + 局限 + 后续工作。

## 6. 核心能力升级

### P0：风格与视觉多样性

#### 6.1 Style Brief

新增 `style_brief`，由 Codex 根据用户选择和材料类型生成：

- style_family：风格族。
- palette：背景、主色、强调色、文字色。
- typography：标题、正文、数字、代码字体。
- density：稀疏、平衡、信息密集。
- image_treatment：全幅、卡片、裁切、黑白、边框。
- layout_rhythm：留白、对齐、页间节奏。
- visual_motif：网格、纸张、实验室、数据、品牌几何等。

#### 6.2 首批六种风格族

| 风格族 | 适用场景 | 视觉特征 |
| --- | --- | --- |
| Academic Light | 论文答辩、组会 | 白底、蓝灰、图表优先、引用清晰 |
| Editorial Paper | 研究总结、综述 | 大标题、杂志式留白、跨页视觉 |
| Tech Dark | 工程技术、AI 产品 | 深色、荧光强调、流程和指标突出 |
| Data Lab | 实验报告、数据复盘 | 图表、数字、网格、对齐轴 |
| Executive Minimal | 管理层汇报 | 大数字、少文字、强结论 |
| Brand Narrative | 产品发布、方案宣讲 | 品牌色、全幅视觉、章节过渡页 |

同一份论文在不同风格下必须产生不同的页面节奏、背景、字体组合和视觉构图，而不是只替换一个主色。

#### 6.3 布局扩展

新增布局注册表：

- `hero-image`：全幅主图 + 标题。
- `figure-caption`：论文图 + 图注 + 结论。
- `chart-focus`：一张主图 + 关键数字。
- `timeline`：时间线或研究演进。
- `matrix`：方法/任务/指标矩阵。
- `two-column-evidence`：左侧主张，右侧证据。
- `quote-source`：关键原文引用与来源页。
- `section-divider`：章节过渡页。
- `case-study`：场景、问题、结果三段式。

#### 6.4 自定义主题模板的产品约束

自定义主题不是“背景 + 字体 + 表格皮肤”的组合，而是由**主题层、页面层、组件层**共同组成的可执行模板系统。

| 层级 | 必须定义 | 不允许只定义 |
| --- | --- | --- |
| 主题层 | 背景策略、配色 token、字体组合、字号层级、间距、边框/圆角、图片处理、表格/图表处理、禁用项 | 仅主色或单张背景图 |
| 页面层 | 至少 5 个布局族；各自的内容槽位、主视觉位置、图片上限、数据位和阅读顺序 | 所有页面共用左右两列卡片 |
| 组件层 | 标题、副标题、短要点、数字指标、图片框、图注、来源脚注、原生表格、原生图表、标签与连接线 | 可无限扩张的大段正文框 |

**图片预算**（图片指论文 Figure、产品截图、实拍/素材图和生成插图；图标、形状、原生图表、原生表格不计入）：

| 页面类型 | 建议图片数 | 硬上限 | 规则 |
| --- | ---: | ---: | --- |
| 封面 / `hero-image` | 1 | 1 | 只允许一张主视觉；没有可靠素材时可为 0，改用抽象图形或大标题 |
| 图文叙事 / `figure-caption` / `case-study` | 1–2 | 2 | 必须存在主图与明确图注/结论的对应关系 |
| 证据拼贴 / 失败案例 | 2–3 | 3 | 只允许 1 张主图 + 最多 2 张辅助图；辅助图必须服务比较、时间或因果关系 |
| 方法、流程、时间线、矩阵 | 0–1 | 1 | 优先原生流程图/结构图，不能为装饰而塞截图 |
| 数据图表、复杂表格 | 0–1 | 1 | 原生 chart/table 优先；源图仅作证据缩略图或可追溯性说明 |
| 结论、章节过渡、引用 | 0–1 | 1 | 允许纯文字/大数字页面，以形成页间节奏 |

任何单页超过 3 张图片必须拒绝渲染并回退到“拼贴页面”重新规划；一张图片在页面中只能承担一种主角色：`hero`、`evidence`、`comparison` 或 `context`。

**文字、表格与背景规则**：

- 标题必须表达页面主张，最多 2 行；章节名不能替代结论标题。
- 正文只允许短要点、短句、图注或来源脚注；长段文字应进入 speaker note 或附录。
- 每页只允许一个一级焦点；标题、主视觉、主图表和大数字不能同时争抢焦点。
- 原生表格默认不超过 6 列和 7 个数据行；超出时应拆分、转图表或转附录，不得靠缩小字号解决。
- 原生图表和表格必须沿用主题的颜色、表头、数字精度和单位格式；颜色不得是唯一的区分通道。
- 背景只能承担层级、情绪或品牌识别，不能降低对比度，也不能替代证据图片；同一主题可有浅/深变体，但页面不得随意更换背景语言。
- 主题层必须提供至少 4 个可交替使用的布局族，连续两页不得使用相同的轮廓，除非用户显式锁定。

**后续 QA 门禁**：

```text
TEMPLATE_IMAGE_LIMIT       单页图片数 <= 3
TEMPLATE_IMAGE_ROLE        每张图片有明确语义角色，且至少 1 个页面主张与其关联
TEMPLATE_TEXT_DENSITY      标题/正文/图注满足可读密度，长段文字进入讲稿
TEMPLATE_TABLE_DENSITY     表格没有靠缩小字体硬塞；超过阈值则拆分或转附录
TEMPLATE_BACKGROUND        背景与文字对比合格，背景不承担事实信息
TEMPLATE_LAYOUT_VARIETY    全套至少使用 4 个布局族，且不连续重复同一轮廓
```

这些规则先作为产品验收标准写入规划；下一阶段再将它们固化为 `style_brief`、`deck.spec.json` 和 `qa.mjs` 的可执行检查。

### P0：论文视觉资产管线

新增 `asset` 模型：

```json
{
  "id": "fig-03",
  "type": "image",
  "source": "pdf",
  "source_page": 7,
  "caption": "Success rate comparison",
  "path": "assets/fig-03.png",
  "alt": "不同方法的成功率比较",
  "role": "main-evidence"
}
```

资产管线需要支持：

- 从 PDF 提取嵌入图片。
- 识别 Figure、Table 和图注。
- 将图注与正文结论建立关联。
- 将表格重建为可编辑 PPTX 表格。
- 将简单数据图重绘为可编辑柱状图、折线图或散点图。
- 无合适原图时生成流程图、示意图或背景插图。
- HTML 使用 `<img>`、SVG 或 Canvas，PPTX 使用可移动、可裁切的原生图片对象。

### P1：可控编辑闭环

支持以下操作：

- “只换这一页风格”。
- “保留图片，只重写标题”。
- “把这张图改成白底”。
- “把两列改为时间线”。
- “锁定第 3 页，不参与后续重生成”。
- “使用论文第 7 页的 Figure 3 替换当前图片”。
- 页面版本对比与撤销。

### P1：质量评分与修订建议

新增四类自动评分：

1. 内容覆盖率：是否覆盖用户要求和论文章节。
2. 视觉证据率：有多少页面包含图片、图表、流程或关键数字。
3. 风格一致性：颜色、字体、间距和图形语言是否统一。
4. 风格差异度：与上一次生成相比，是否真正改变了布局和视觉策略。

评分只用于给出可执行建议，不直接替用户覆盖内容。

## 7. 技术架构演进

```text
Source Loader
  ├── PDF text extractor
  ├── PDF figure/table extractor
  └── user asset loader
          ↓
Evidence Index
  ├── text chunks
  ├── source pages
  ├── figures / tables
  └── captions / claims
          ↓
Codex Planner
  ├── narrative plan
  ├── style brief
  ├── asset plan
  └── editable spec
          ↓
Layout Engine
  ├── layout registry
  ├── density checker
  ├── style tokens
  └── asset placement
          ↓
HTML Renderer + PPTX Renderer
          ↓
Visual QA + Source QA + User Revision
```

### 7.1 Schema 方向

`deck.schema.json` 应从“页面内容 schema”升级为“内容 + 视觉 + 证据 schema”：

- `theme`：style family、tokens、字体、背景策略。
- `assets`：图片、图表、表格、图注、来源页。
- `slides`：layout、claim、evidence_refs、elements。
- `elements`：text、shape、connector、image、chart、table、quote。
- `review_flags`：内容、视觉、来源和可编辑性问题。

### 7.2 Prompt 与验证

遵循以下工程规则：

- Prompt 版本化，记录 style brief 和 layout 选择原因。
- Codex 只输出严格 schema JSON。
- 对图片来源页、数字和结论执行来源校验。
- 不把整篇 PDF 无差别塞入每一次调用；先建立带页码的证据索引，再按页面检索相关内容。
- 每个页面先生成“视觉计划”，再执行渲染。
- 失败时保留中间产物，不能伪造生成成功。

## 8. 分阶段路线图

### 版本 0.2：摆脱单一模板

目标：让用户第一次看到明显的风格差异。

- 6 种风格族。
- 6 个新布局。
- `style_brief` schema。
- 用户选择风格和信息密度。
- PPTX/HTML 统一读取 style tokens。
- 增加风格差异度 QA。

### 版本 0.3：让论文有图、有证据

目标：让论文 PPT 不再是“纯文字摘要”。

- PDF Figure/Table/Caption 提取。
- `image`、`chart`、`table` 元素。
- Figure 引用页码。
- 论文原图与可编辑重绘图双模式。
- 图表页面模板和图注模板。

### 版本 0.4：让用户愿意反复修改

目标：把“一次生成工具”变成“可持续创作工具”。

- 页面级局部重生成。
- 元素锁定和版本回退。
- 参考 PPT/品牌色导入。
- 个人风格收藏和历史复用。
- 一键生成“同风格新 deck”。

### 版本 0.5：形成产品壁垒

- 基于用户修改记录学习风格偏好。
- 多人审阅和批注。
- 讲稿、演讲者备注和问答卡片。
- PPTX 编辑结果回流为下一次生成约束。
- 团队品牌资产和模板治理。

## 9. 指标体系

### 北极星指标

每周完成“生成 → 预览 → 局部修改 → 导出”的有效 deck 数量。

### 激活指标

- 首次上传到看到预览的中位时间 ≤ 3 分钟。
- 首次生成后 HTML 打开率 ≥ 80%。
- 首次生成后 PPTX 导出率 ≥ 50%。

### 质量指标

- 论文材料中有图表时，视觉证据页面占比 ≥ 40%。
- 人工盲评“风格明显不同”的生成结果 ≥ 80%。
- 首次渲染无溢出/重叠通过率 ≥ 95%。
- 数字和结论可追溯到来源页的比例 ≥ 95%。

### 留存指标

- 生成后 24 小时内发生局部修改的用户比例 ≥ 35%。
- D7 再次生成或复用风格的用户比例 ≥ 25%。
- 用户平均生成版本数 ≥ 2.5。

## 10. MVP+ 验收标准

以下条件全部满足后，才认为产品摆脱了“固定模板 MVP”：

- 同一份 PDF 使用三种风格，封面、背景、字体组合和布局节奏均有明显差异。
- 论文中存在 Figure/Table 时，至少 40% 的正文页使用真实视觉资产或可编辑重绘图。
- PPTX 中图片可以移动、缩放、裁切；标题、正文、图注和图表仍可编辑。
- 每个视觉资产保留来源页和图注信息。
- 用户可以锁定页面并只重生成其他页面。
- HTML 与 PPTX 共享同一份 style tokens 和 asset refs。
- 自动检查覆盖溢出、重叠、来源缺失、视觉证据不足和风格重复。
- 20 份不同领域论文的回归测试中，首轮通过率达到 90%。

## 11. 首个开发迭代建议

下一轮不建议直接做复杂的图片生成，而应优先完成以下闭环：

1. `style_brief` 和 6 个主题 token。
2. `image` 元素及 PDF 图片提取。
3. `figure-caption`、`chart-focus`、`hero-image` 三个布局。
4. 页面级“只重生成这一页”。
5. 风格差异度和视觉证据率 QA。

这五项完成后，用户才能明显感知到产品从“固定模板生成器”变成“可控的 AI PPT 设计工具”。

## 12. 结论

当前 MVP 已经验证了本地 Codex、统一 schema、HTML 和可编辑 PPTX 双渲染链路。下一阶段的关键不是继续增加文字模板，而是补齐三项核心能力：

1. **视觉决策**：AI 能选择并解释风格与布局。
2. **视觉证据**：AI 能使用论文原图、图表和来源。
3. **可控迭代**：用户能锁定、局部修改、复用和积累自己的风格。

只有当用户能够看到真正不同的结果、快速修正结果，并在下一次生成中复用自己的偏好，产品才会从一次性 Demo 变成值得长期使用的办公生产力工具。
